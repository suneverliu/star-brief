interface:
  display_name: "STAR Brief"
  short_description: "STAR brief from specs and changes."
  default_prompt: "Use $star-brief to produce a concise, practical STAR project brief from the workspace specs and latest code changes, in the same language as the user's request."
